using UnityEngine;
using System.Collections;

public class ResetError : MonoBehaviour {

	// Use this for initialization
	void Start () {

        PlayerPrefs.SetString("Error Display", " ");

    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
